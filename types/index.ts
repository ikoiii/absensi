/**
 * Shared TypeScript Types and Interfaces
 * 
 * Re-export all types from this barrel file for convenient imports
 */

export type { Database } from './database';

// Additional type definitions will be added as needed
// Examples:
// - Auth types (User, Session, Profile)
// - Session types (Session, CreateSessionInput)
// - Attendance types (Attendance, AttendanceRecord)
